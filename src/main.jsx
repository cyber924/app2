import React from 'react';
import ReactDOM from 'react-dom/client';
import PromptRecommender from './PromptRecommender';
import './style.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PromptRecommender />
  </React.StrictMode>
);
